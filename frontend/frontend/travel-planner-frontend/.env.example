# Must match backend/.env's GOOGLE_CLIENT_ID exactly. Leave blank to run without
# Google sign-in — the button just won't render.
VITE_GOOGLE_CLIENT_ID=

# Only needed if the backend isn't running at the default http://localhost:8000
VITE_API_URL=
